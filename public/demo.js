export const demoPlan = {
  id: 'python-starter', title: 'Python 入门：从第一行代码到小工具',
  goal: '掌握 Python 基础，独立写出一个日常记账小工具', level: '零基础', daily: 25, days: 14, source: 'demo',
  description: '用 6 节短课建立编程思维，在每一次动手练习中，把“看懂了”变成“会用了”。',
  lessons: [
    { id: 'p1', title: '与 Python 的第一次对话', objective: '理解输入、计算与输出，读懂第一段程序', phase: '建立基础', minutes: 15, tags: ['Python', '输入输出'] },
    { id: 'p2', title: '变量与数据：给信息一个名字', objective: '用变量保存信息，分清字符串、整数与浮点数', phase: '建立基础', minutes: 25, tags: ['Python', '变量', '数据类型'] },
    { id: 'p3', title: '条件判断：让程序学会选择', objective: '使用 if / elif / else 表达业务规则', phase: '掌握方法', minutes: 25, tags: ['Python', '条件判断'] },
    { id: 'p4', title: '列表与循环：把重复交给程序', objective: '存储多条数据，用 for 循环完成批量计算', phase: '掌握方法', minutes: 30, tags: ['Python', '列表', '循环'] },
    { id: 'p5', title: '函数：把好方法反复使用', objective: '用参数和返回值封装可复用的逻辑', phase: '动手应用', minutes: 25, tags: ['Python', '函数'] },
    { id: 'p6', title: '综合实践：我的记账小工具', objective: '结合变量、列表、循环与函数完成一个小项目', phase: '动手应用', minutes: 35, tags: ['Python', '项目实践', '函数'] }
  ]
};

const lesson = (intro, sections, example, challenge, questions, takeaways) => ({ intro, sections, example, challenge, questions, takeaways });
const q = (prompt, options, answer, explanation) => ({ prompt, options, answer, explanation });
export const demoLessons = {
  p1: lesson('程序是写给计算机的一组明确指令。今天从最小的一步开始：让 Python 帮你计算，再把结果展示出来。', [
    { heading: '01 / 先认识输入、处理、输出', body: '想象一台咖啡机：你选择饮品（输入），机器制作（处理），最后拿到咖啡（输出）。程序也遵循这个过程。Python 的 print() 负责把信息显示出来。' },
    { heading: '02 / 文字与计算有什么不同？', body: '引号里的内容是字符串，会按文字显示；没有引号的 2 + 3 是一个表达式，Python 会先求值，得到数字 5，再交给 print()。英文括号和引号必须成对出现。' },
    { heading: '03 / 建立“预测 → 验证”的习惯', body: '读代码时先在脑中预测输出，再到自己的 Python 环境运行。预测与结果的差异，就是最有价值的学习机会。本工具中的代码区用于阅读，不会执行代码。' }
  ], 'print("你好，Python！")\nprint(2 + 3)\nprint("2 + 3")\n\n# 输出：\n# 你好，Python！\n# 5\n# 2 + 3', '在自己的 Python 环境中输出你的名字，并计算每天学习 25 分钟、一周 7 天的总时长。先写下预测结果。', [
    q('print(2 + 3) 的输出是什么？', ['2 + 3', '5', '"5"', '没有输出'], 1, '括号内的算术表达式先求值，得到数字 5，print 再显示这个结果。'),
    q('哪一行会原样输出文字 2 + 3？', ['print(2 + 3)', 'print("2 + 3")', 'print(5)', '2 + 3'], 1, '加上引号后，内容成为字符串，不再执行加法。')
  ], ['print() 用于显示信息。', '引号中的内容是字符串；算术表达式会先计算再输出。', '先预测再验证，比只阅读代码更容易发现理解偏差。']),
  p2: lesson('变量让程序能够记住信息。你可以把变量理解为贴在某个值上的标签，后续通过名字就能使用这个值。', [
    { heading: '01 / 用名字表达意义', body: 'minutes = 25 把整数 25 绑定到名字 minutes。等号是赋值，不是数学里的相等判断。好的变量名能说明含义，例如 daily_minutes 比 x 更容易理解。' },
    { heading: '02 / 三种常见数据类型', body: 'int 表示整数，例如 25；float 表示带小数的数，例如 3.5；str 表示文字，例如 "25"。整数 25 与字符串 "25" 不相同。两个字符串相加会拼接，而数字相加才是算术运算。' },
    { heading: '03 / 读取输入时注意类型', body: 'input() 的结果始终是字符串。计算前可用 int() 转换为整数；但 "你好" 不能直接转成整数，会引发 ValueError。接收真实用户输入时，应考虑校验与异常处理。' }
  ], 'daily_minutes = 25\ndays = 7\ntotal = daily_minutes * days\nprint(total)  # 175\n\nprint("25" + "7")  # 257\nprint(int("25") + 7)  # 32', '定义你的学习天数和每日分钟数，计算总学习时长。再解释为什么 "25" + "7" 不等于 32。', [
    q('"10" + "5" 的结果是什么？', ['15', '"105"', '50', '报错'], 1, '两个字符串使用 + 会进行拼接，得到字符串 "105"。'),
    q('input() 返回的值默认是什么类型？', ['整数', '浮点数', '字符串', '布尔值'], 2, '即使用户输入数字，input() 返回的仍然是字符串，需要按需转换。')
  ], ['变量名绑定一个值，= 用于赋值。', '字符串与数字是不同类型，运算行为不同。', 'input() 返回字符串；类型转换要考虑无效输入。']),
  p3: lesson('现实中的决策往往都有条件。用 if 语句，你可以把这些条件写成清晰、可以执行的规则。', [
    { heading: '01 / 判断条件是否成立', body: 'if 后面的表达式会得到 True 或 False。大于等于写作 >=，判断相等写作 ==。冒号后的代码块需要缩进，通常使用 4 个空格。' },
    { heading: '02 / 在多个分支之间选择', body: 'if / elif / else 从上到下检查，只执行第一个满足条件的分支。else 处理前面条件都不满足的情况。比较具体或门槛较高的条件通常需要先检查。' }
  ], 'minutes = 30\nif minutes >= 25:\n    print("今天的目标达成！")\nelif minutes > 0:\n    print("已经迈出了第一步")\nelse:\n    print("从 5 分钟开始吧")', '编写一个学习时长提醒：达到 60 分钟提示充分学习，达到 25 分钟提示达标，否则提示继续加油。注意分支顺序。', [
    q('判断两个值是否相等应使用哪个符号？', ['=', '==', '!=', '=>'], 1, '== 是相等比较；= 是赋值。'),
    q('如果 if 和后面的 elif 条件都为真，会执行哪些分支？', ['两个都执行', '只执行 if 分支', '只执行 elif 分支', '都不执行'], 1, '同一 if / elif 链只执行首个满足条件的分支。')
  ], ['if 根据布尔条件选择执行路径。', 'if / elif / else 只执行首个命中的分支。', '缩进定义代码块；== 比较相等，= 用于赋值。']),
  p4: lesson('当你有一周的支出或一整月的学习记录，列表与循环能帮助你批量处理这些信息。', [
    { heading: '01 / 用列表保存一组值', body: '列表用方括号表示，例如 [12, 18, 9]。元素按顺序排列，下标从 0 开始；expenses[0] 取第一个值。使用 append() 可以向列表末尾添加元素。' },
    { heading: '02 / 用 for 逐个处理', body: 'for amount in expenses 会依次把每个元素绑定给 amount，然后执行缩进的代码。累加器 total 要在循环前初始化，这样每次循环都能保留之前的结果。' }
  ], 'expenses = [12, 18, 9]\ntotal = 0\nfor amount in expenses:\n    total = total + amount\nprint(total)  # 39\n\n# Python 也提供内置求和：\nprint(sum(expenses))', '把一周的学习时长保存在列表中，用循环计算总时长，并数一数有多少天达到了 25 分钟。', [
    q('列表 [12, 18, 9] 中，下标 0 对应哪个元素？', ['12', '18', '9', '不存在'], 0, 'Python 列表的下标从 0 开始，所以第一个元素是 12。'),
    q('为什么 total = 0 应写在求和循环之前？', ['让代码更长', '避免每轮把累计结果清零', '列表要求这样写', '没有原因'], 1, '放在循环体内会每次重置累计值，无法得到所有元素的总和。')
  ], ['列表有顺序，下标从 0 开始。', 'for 循环逐个处理可迭代对象中的元素。', '累计变量应在循环之前初始化。']),
  p5: lesson('函数把一段逻辑包装成一个有名字的工具。给它输入，拿到输出，就可以在不同场景里重复使用。', [
    { heading: '01 / 参数是工具的输入', body: 'def 定义函数，函数名后面的括号声明参数。调用时传入实参，例如 average([10, 20])。函数体同样需要缩进。' },
    { heading: '02 / 返回结果和打印结果不同', body: 'return 把结果交给调用者，后续可以继续计算；print 只是显示信息。如果没有显式 return，函数会返回 None。空列表不能直接用总和除以长度，需要提前处理。' }
  ], 'def average(values):\n    if len(values) == 0:\n        return 0\n    return sum(values) / len(values)\n\nresult = average([10, 20, 30])\nprint(result)  # 20.0', '写一个 is_goal_met(minutes) 函数，返回学习时长是否至少为 25 分钟。用 0、24、25、60 四个输入检查边界。', [
    q('希望函数结果可用于后续计算，应该使用什么？', ['print', 'return', 'input', 'elif'], 1, 'return 将值传回调用者；print 只负责显示。'),
    q('计算列表平均值之前，为什么要判断列表是否为空？', ['空列表不能打印', '避免除以零', '空列表没有类型', '为了排序'], 1, '空列表的长度为 0，直接 sum(values) / len(values) 会除以零。')
  ], ['def 定义函数，参数让逻辑可复用。', 'return 返回结果，print 显示结果。', '空输入与临界值是重要的测试场景。']),
  p6: lesson('把前面学到的知识连接起来：用列表保存支出，通过函数生成一份简单统计。这是一个可以逐步扩展的小工具。', [
    { heading: '01 / 先描述任务，再拆分步骤', body: '输入是一组支出数字；处理是计算总额、笔数与平均值；输出是清晰的统计结果。先把这三部分写下来，再开始编码，会更容易发现缺漏。' },
    { heading: '02 / 检查正常与特殊输入', body: '至少检查三类情况：多笔支出、只有一笔、空列表。这个教学版本假定数据已经是非负数字。若扩展到用户输入，还需要验证格式、拒绝负数，并处理无效内容。' },
    { heading: '03 / 为下一次使用留下解释', body: '完成后记录：工具解决什么问题、采用什么方法、哪些情况还不支持。把这些信息沉淀到 Wiki，下次遇到相似任务时，可以找到方法及其使用边界。' }
  ], 'def report(expenses):\n    total = sum(expenses)\n    count = len(expenses)\n    average = total / count if count else 0\n    return total, count, average\n\ntotal, count, average = report([12, 18, 9])\nprint("总支出：", total)\nprint("笔数：", count)\nprint("平均支出：", average)', '在自己的 Python 环境实现记账统计，并测试 []、[10]、[12, 18, 9]。进阶挑战：添加预算判断，显示是否超出预算。', [
    q('对空列表 []，示例 report 返回什么？', ['(0, 0, 0)', '报错', '(0, 1, 0)', 'None'], 0, 'count 为 0 时走条件表达式的 else 分支，平均值设为 0。'),
    q('把工具扩展为接收用户输入时，应首先补充什么？', ['随机打乱数据', '输入验证和无效值处理', '去掉所有函数', '只支持一笔支出'], 1, '真实输入可能不是数字或包含负数，需要明确规则并验证。')
  ], ['用输入、处理、输出拆解任务。', '结合列表、函数与分支实现可复用统计。', '测试正常输入、单值与空输入；记录工具的假设和边界。'])
};
